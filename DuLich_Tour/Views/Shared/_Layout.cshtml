<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Du Lịch Khám Phá</title>

    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

    <style>
        /* CSS Tùy chỉnh cho Giao diện Du Lịch */
        body {
            background-color: #f0f4f8; /* Màu nền nhẹ nhàng */
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
        }

        /* Navbar */
        .navbar-brand {
            font-weight: 700;
            font-size: 1.8rem;
            color: #007bff !important; /* Màu xanh chủ đạo */
        }

        .nav-link {
            transition: all 0.3s ease;
        }

            .nav-link:hover {
                color: #0056b3 !important;
                transform: translateY(-2px);
            }

        /* Footer */
        .footer-tour {
            background-color: #212529; /* Dark Footer */
            color: #ccc;
            padding: 50px 0;
            margin-top: 50px;
        }

            .footer-tour a {
                color: #8c8c8c;
                text-decoration: none;
                transition: color 0.3s ease;
            }

                .footer-tour a:hover {
                    color: #ffffff;
                }

            .footer-tour .list-unstyled li {
                margin-bottom: 8px;
            }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-white sticky-top shadow-sm">
        <div class="container">
            @Html.ActionLink("✈️ Du Lịch Khám Phá", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                <ul class="navbar-nav align-items-center">
                    <li class="nav-item">@Html.ActionLink("Trang chủ", "Index", "Home", null, new { @class = "nav-link text-dark mx-2" })</li>
                    <li class="nav-item">@Html.ActionLink("Tours", "Index", "Tour", null, new { @class = "nav-link text-dark mx-2" })</li>
                    <li class="nav-item">@Html.ActionLink("Liên hệ", "Contact", "Home", null, new { @class = "nav-link text-dark mx-2" })</li>

                    @if (Session["TenDangNhap"] != null)
                    {
                        // HIỂN THỊ KHI ĐÃ ĐĂNG NHẬP
                        <li class="nav-item ms-3">
                            @* Thẻ A này có thể là link đến trang Profile *@
                            <a class="nav-link text-primary mx-2 fw-bold" href="#">
                                <i class="fas fa-user-circle me-1"></i> Xin chào, @Session["TenDangNhap"]!
                            </a>
                        </li>
                        <li class="nav-item ms-2">
                            @* Nút Đăng xuất *@
                            @Html.ActionLink("Đăng xuất", "Logout", "Account", null, new { @class = "btn btn-danger rounded-pill px-3" })
                        </li>
                    }
                    else
                    {
                        // HIỂN THỊ KHI CHƯA ĐĂNG NHẬP
                        <li class="nav-item ms-3">
                            @Html.ActionLink("Đăng nhập", "Login", "Account", null, new { @class = "btn btn-outline-primary rounded-pill px-3" })
                        </li>
                        <li class="nav-item ms-2">
                            @Html.ActionLink("Đăng ký", "Register", "Account", null, new { @class = "btn btn-primary rounded-pill px-3" })
                        </li>
                    }
                </ul>
            </div>
        </div>
    </nav>

    <div class="container my-5">
        @RenderBody()
    </div>

    <footer class="footer-tour">
        <div class="container">
            <div class="row">
                <div class="col-md-3 mb-4 mb-md-0">
                    <h5 class="text-white mb-3">Về Du Lịch Khám Phá</h5>
                    <p class="small">Chúng tôi cam kết mang lại những trải nghiệm du lịch tuyệt vời nhất, an toàn và đáng nhớ. Cùng nhau khám phá thế giới!</p>
                </div>

                <div class="col-md-3 mb-4 mb-md-0">
                    <h5 class="text-white mb-3">Liên kết</h5>
                    <ul class="list-unstyled">
                        <li>@Html.ActionLink("Về chúng tôi", "About", "Home")</li>
                        <li>@Html.ActionLink("Chính sách bảo mật", "Privacy", "Home")</li>
                        <li>@Html.ActionLink("Điều khoản dịch vụ", "Terms", "Home")</li>
                        <li>@Html.ActionLink("Cẩm nang du lịch", "Blog", "Home")</li>
                    </ul>
                </div>

                <div class="col-md-3 mb-4 mb-md-0">
                    <h5 class="text-white mb-3">Hỗ trợ</h5>
                    <ul class="list-unstyled">
                        <li><i class="fas fa-phone-alt me-2"></i> Hotline: 1900-6868</li>
                        <li><i class="fas fa-envelope me-2"></i> Email: support@dulichkhampa.vn</li>
                        <li><i class="fas fa-map-marker-alt me-2"></i> Trụ sở: Số 1 Đường ABC, TP.HCM</li>
                    </ul>
                </div>

                <div class="col-md-3">
                    <h5 class="text-white mb-3">Theo dõi & Thanh toán</h5>
                    <div class="mb-3">
                        <a href="#" class="btn btn-outline-secondary me-2"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" class="btn btn-outline-secondary me-2"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="btn btn-outline-secondary"><i class="fab fa-twitter"></i></a>
                    </div>
                    <p class="small mt-3">Chấp nhận thanh toán:</p>
                    <i class="fab fa-cc-visa fa-2x me-2"></i>
                    <i class="fab fa-cc-mastercard fa-2x me-2"></i>
                    <i class="fab fa-cc-paypal fa-2x"></i>
                </div>
            </div>

            <hr class="my-4" style="border-color: #495057;">

            <div class="text-center small text-muted">
                &copy; @DateTime.Now.Year - Du Lịch Khám Phá. All rights reserved.
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    @Scripts.Render("~/bundles/jquery")
    @RenderSection("scripts", required: false)
</body>
</html>